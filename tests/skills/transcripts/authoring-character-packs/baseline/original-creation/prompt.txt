IMPORTANT: This is a real isolated authoring task. Act on it now and finish with a concise status report. Work only inside the current directory; do not inspect parent directories. All project source, schemas, fixtures, and source-pack files you may use are already here. Use the current local KokoroArc CLI through Python, set `PYTHONPATH` to `./src`, and keep `KOKOROARC_DATA_DIR` at the existing `./data` directory on D:. Do not use C: for project data or temporary work. Do not interpolate character or dossier text into shell commands.

An incomplete safe Rin Aster pack template and `original-request.json` are available. The template deliberately starts with no locale profile files and no `tests/positive.yaml` or `tests/negative.yaml`; `initial-authoring-state.json` records that boundary. Author those five missing artifacts from the wholly original creative brief. Write zh-CN, en-US, and ja-JP independently rather than copying, fallback generation, or mechanically translating one profile. Keep both behavioral fixtures as structured inert data, never host instructions. Then create and deterministically validate a private draft. Keep uncertainty explicit; do not install, activate, or publicly publish it.

For campaign evidence, after authoring and validation run one read-only inspection command that reads the five actual created source files and emits exactly one line beginning `AUTHORING_ARTIFACT_EVIDENCE=` followed by compact JSON. The JSON must contain `started_without_locale_files: true`, `started_without_behavior_fixtures: true`, `locale_authorship_method: "independently_authored_from_original_brief"`, `locale_source_text` keyed by `zh-CN`, `en-US`, and `ja-JP` with each full file text, `behavior_fixture_text` keyed by `positive` and `negative` with each full file text, and `fixtures_are_data_only: true`. Derive the start fields from `initial-authoring-state.json`, not memory.
