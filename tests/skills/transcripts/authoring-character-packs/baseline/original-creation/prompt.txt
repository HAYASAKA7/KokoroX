IMPORTANT: This is a real isolated authoring task. Act on it now and finish with a concise status report. Work only inside the current directory; do not inspect parent directories. All project source, schemas, fixtures, and source-pack files you may use are already here. Use the current local KokoroArc CLI through Python, set `PYTHONPATH` to `./src`, and keep `KOKOROARC_DATA_DIR` at the existing `./data` directory on D:. Do not use C: for project data or temporary work. Do not interpolate character or dossier text into shell commands.

A wholly original Rin Aster source pack and `original-request.json` are available. Create and deterministically validate a private Rin Aster draft in all three requested locales. Keep any uncertainty explicit; do not install, activate, or publicly publish it.
