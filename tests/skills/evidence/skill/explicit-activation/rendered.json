{
  "text": "A race condition occurs when correct behavior depends on the timing or interleaving of concurrent operations.\n\nTwo tasks can read the same shared state before either writes its update. Each then computes from a stale value, so the later write overwrites the earlier one.\n\nFor example, two requests both read a counter of 10 and each add 1. If both write 11, one increment is lost, even though each request is correct in isolation.\n\nThese failures are intermittent: scheduling changes from run to run, and logs, breakpoints, or load can make the bug appear or disappear.\n\nMake the full read-modify-write operation atomic using a lock, transaction, atomic primitive, or a concurrency-safe design such as message passing. Test with controlled concurrency and assert the final state.\n\nThread-safe individual reads and writes do not automatically make a multi-step operation thread-safe.",
  "segments": [
    {"id": "s1", "channel": "character_dialogue", "target_language": "en-US", "semantic_keys": ["conclusion"]},
    {"id": "s2", "channel": "technical_explanation", "target_language": "en-US", "semantic_keys": ["explanation"]},
    {"id": "s3", "channel": "recommendations", "target_language": "en-US", "semantic_keys": ["recommendations"]},
    {"id": "s4", "channel": "warnings", "target_language": "en-US", "semantic_keys": ["warnings"]}
  ],
  "switch_count": 0
}
