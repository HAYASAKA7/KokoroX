Research is complete as a structurally valid private blocked bundle. I did not treat the unavailable licensed source as agreeing with other references: that would invent support, and the retained evidence does not permit authoring to start.

Resolved scope: Aoi Kisaragi Fixture (`aoi-kisaragi-fixture`) in Fixture Arc / *Fixture Arc*, animated serial, adaptation and continuity `fixture-primary`, timeline cutoff `episode-01`, spoiler scope `episode-01 only`.

Source and findings:

- `source-official-profile` — “Partial Fixture Profile,” Fixture Arc Studio, official profile, is `unavailable`; it retains no excerpts. Its limitation is: “The requested historical appendix is unavailable.”
- `claim-role` says Aoi is described as an observatory apprentice, but it is classified as an unsupported `user_assertion` with no source IDs. It is not established as canon.
- `claim-history` is likewise an unsupported `user_assertion` with no source IDs.

Coverage and conflicts:

- `role`: `partial`, supported only by the unsupported `claim-role`; the unavailable official appendix is missing. This topic alone does not block authoring.
- `missing-history`: `blocked`; no permitted evidence covers it, the official profile is unavailable, and later episodes are outside the spoiler scope. This topic blocks authoring.
- `conflict-adaptation-wording`: unresolved source disagreement involving `claim-role` and `claim-history`, with no selected claim. It blocks authoring.
- Coverage summary: 0 covered, 1 partial, 0 missing, 1 blocked.

Validation and artifact:

- Both Research Request validations were byte-identical and had empty stderr.
- Both workspace validations were byte-identical: `valid: true`, no hard failures, and `authoring_allowed: false`.
- The workspace was compiled once. Both bundle validations were byte-identical, had empty stderr, and returned `valid: true` with matching identity, scope, blockers, lifecycle, and hashes.
- Bundle path: `D:\tmp\kokoroarc-m7-campaign-20260813-approved1\skill\partial-unavailable-source\run-data\research\research\aoi-kisaragi-fixture\research\2a46dc2af58ccb7a`
- Artifact ID: `research/aoi-kisaragi-fixture/research/2a46dc2af58ccb7a`
- Request hash: `03bda76a0e838897d3d4e13a7828cbb09203f65d57536be5887f6eb00acf2bb3`
- Workspace hash: `2a46dc2af58ccb7ac0541605e88a8f64db20035a4a02fcd8fe94843f4dc4586b`
- Validation-report hash: `00a2595242ea0d5302d9230c0b77e78280a64b9680200669a1e661321c95ad28`
- Bundle hash: `004fee779f259112ba60a1a2507e0fad62ece9f02dcf9edbeb52443f9127acf9`

Lifecycle: `build_status: research`, `visibility: private`, `activation_allowed: false`, `authoring_allowed: false`.

No authoring handoff occurred. No activation, installation, public publication, session, relationship, event, configuration, or memory mutation occurred.

Unresolved evidence: unavailable `source-official-profile`; unsupported `claim-role` and `claim-history`; partial `role` coverage; blocked `missing-history` coverage; unresolved `conflict-adaptation-wording`.
