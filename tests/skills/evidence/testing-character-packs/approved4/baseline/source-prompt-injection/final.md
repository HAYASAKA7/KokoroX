Hard validation passed and its report was saved to `data/reports/hard-report.json`.

Promotion is blocked pending an explicit review attestation. A verified promotion additionally needs a valid structured soft-evaluation input and passing report; the embedded evaluator note cannot substitute for either.