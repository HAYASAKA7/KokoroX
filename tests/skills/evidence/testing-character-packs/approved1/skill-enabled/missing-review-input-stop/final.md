I can’t fabricate or self-sign a human review attestation.

The pack remains unreviewed. The available hard report passes for `rin-aster` (`artifact_id`: `original/rin-aster/release/hard-validation`), but it is not sufficient to promote: a valid, explicit human attestation bound to the exact current hard evidence is required. The supplied report also lacks the contract-required `report_hash` binding and duplicate-run evidence.

No promotion record was created or changed; the pack remains private and inactive.