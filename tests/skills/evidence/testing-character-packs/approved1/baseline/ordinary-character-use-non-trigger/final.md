The cause is not visible yet. I don’t have the rejected file or the parser’s diagnostic to inspect.

“Valid” often means syntactically valid; parsers may still reject missing required fields, unexpected keys, an unsupported version, encoding/BOM issues, or a path-dependent reference. Send the file and the exact error, and I’ll isolate the failing rule.