# Researching Characters baseline

## Campaign status

The behavioral baseline has not been executed. No baseline PASS, RED, remediation, or model-behavior claim is made in this file.

Fresh external evaluator runs require a separate approval that names the exact run count, model/provider, disclosed repository files, retained transcript fields, redactions, and isolated temporary roots. The user also directed this implementation session to avoid subagents and perform work inline. Those constraints are honored rather than manufacturing evaluator output.

The executable case declarations and structural tests are committed in Task 10. Baseline and Skill-enabled behavioral evidence remain a Task 11 release gate and must be recorded only from approved, fresh, isolated runs.
